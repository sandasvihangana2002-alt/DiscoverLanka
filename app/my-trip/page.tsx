"use client";

import { useEffect, useMemo, useState } from "react";

// My Trip dashboard — enhanced in the next pass.

type Place = { id: string; slug: string; name: string; region: string; summary: string; image: string };
type TripData = { days: number; travelers: number; interest: string; budgetLevel: string; selectedSlugs: string[]; itinerary: unknown[] };
type Trip = { id: string; title: string; budget: number | null; currency: string; status: string; data: TripData };
type Itinerary = { id: string; title: string; summary: string | null; duration_days: number; budget_level: string; content: { days?: Array<{ day: number; destination?: { name?: string; region?: string }; focus?: string; items?: Array<{ time?: string; title?: string }> }> } };

const images: Record<string, string> = {};
const rates: Record<string, number> = { Budget: 9000, Comfort: 18000, Premium: 35000 };

export default function MyTripPage() {
  const [saved, setSaved] = useState<string[]>([]);
  const [places, setPlaces] = useState<Place[]>([]);
  const [trip, setTrip] = useState<Trip | null>(null);
  const [itinerary, setItinerary] = useState<Itinerary | null>(null);
  const [loading, setLoading] = useState(true);
  const [copied, setCopied] = useState(false);
  const [error, setError] = useState("");
  useEffect(() => { async function load() { try { const local = JSON.parse(localStorage.getItem("discoverlanka-saved") ?? "[]"); const id = localStorage.getItem("discoverlanka-trip-id"); const r = await fetch("/api/destinations"); const d = r.ok ? await r.json() : { destinations: [] }; const rows = Array.isArray(d.destinations) ? d.destinations : []; setPlaces(rows.map((item: Omit<Place,"image">) => ({...item, image: images[item.slug] ?? images.kandy ?? ""}))); if (id) { const [tr, it] = await Promise.all([fetch(`/api/trips?id=${encodeURIComponent(id)}`), fetch(`/api/itineraries?tripId=${encodeURIComponent(id)}`)]); if (tr.ok) { const td = await tr.json(); if (td?.trip) { setTrip(td.trip as Trip); const sl = Array.isArray(td.trip.data?.selectedSlugs) ? td.trip.data.selectedSlugs : []; setSaved(sl.length ? sl : Array.isArray(local) ? local : []); } } if (it.ok) { const idata = await it.json(); if (idata?.itinerary) setItinerary(idata.itinerary as Itinerary); } } else if (Array.isArray(local)) setSaved(local.filter((v): v is string => typeof v === "string")); } catch { setError("Could not load the live trip right now."); } finally { setLoading(false); } } load(); }, []);
  const selected = useMemo(() => saved.map(slug => places.find(p => p.slug === slug)).filter(Boolean) as Place[], [saved, places]); const data = trip?.data; const days = data?.days ?? itinerary?.duration_days ?? 7; const travelers = data?.travelers ?? 2; const budget = data?.budgetLevel ?? itinerary?.budget_level ?? "Comfort"; const interest = data?.interest ?? "Sri Lanka"; const daily = rates[budget] ?? rates.Comfort; const estimate = trip?.budget ?? daily * days * travelers; const money = (v:number) => `LKR ${Math.round(v).toLocaleString("en-LK")}`; const route = selected.map(p=>p.name).join(" → ");
  async function share(){const text = trip ? `${trip.title} · ${route || "Sri Lanka"} · ${days} days · ${money(Number(estimate))}` : `My DiscoverLanka trip: ${route}`; try { if(navigator.share) await navigator.share({title:"My DiscoverLanka Trip", text}); else {await navigator.clipboard.writeText(text);setCopied(true);setTimeout(()=>setCopied(false),1800)}} catch {}}
  function removeStop(slug:string){const next=saved.filter(v=>v!==slug);setSaved(next);localStorage.setItem("discoverlanka-saved",JSON.stringify(next));}
  function clearLocal(){setSaved([]);setTrip(null);setItinerary(null);["discoverlanka-saved","discoverlanka-trip-id","discoverlanka-itinerary","discoverlanka-plan"].forEach(k=>localStorage.removeItem(k));}
  return <main className="min-h-screen bg-[#f7f5ef] text-[#10251f]"><header className="sticky top-0 z-30 border-b border-black/10 bg-[#f7f5ef]/95 backdrop-blur-md"><div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-4"><a href="/" className="text-xl font-bold">Discover<span className="text-[#d9a441]">Lanka</span></a><div className="flex items-center gap-4"><a href="/plan" className="text-sm font-semibold text-[#66756f]">Plan trip</a><a href="/" className="text-sm font-semibold text-[#66756f]">← Discover</a></div></div></header><section className="mx-auto max-w-7xl px-6 pb-24 pt-16 md:pt-24"><div className="flex flex-col justify-between gap-8 md:flex-row md:items-end"><div><p className="text-sm font-bold uppercase tracking-[.2em] text-[#b27c1d]">My Trip</p><h1 className="mt-3 text-5xl font-semibold tracking-[-.04em] md:text-7xl">Your Sri Lankan journey.</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-[#66756f]">One place for your saved stops, live itinerary, budget and next decisions.</p></div><div className="flex flex-wrap gap-3"><button onClick={share} disabled={!selected.length&&!trip} className="rounded-full border border-black/10 bg-white px-5 py-3 text-sm font-bold disabled:opacity-40">{copied?"Copied ✓":"Share trip"}</button><a href="/plan" className="rounded-full bg-[#183d32] px-6 py-3 text-sm font-bold text-white">Edit trip →</a></div></div>{loading?<div className="mt-14 rounded-[2rem] bg-white p-12 text-center text-[#66756f]">Loading your live trip…</div>:error?<div className="mt-14 rounded-[2rem] bg-[#e9e2d3] p-10 text-center">{error}</div>:!selected.length?<div className="mt-14 rounded-[2rem] bg-[#e9e2d3] p-10 text-center md:p-16"><div className="text-5xl">♡</div><h2 className="mt-5 text-3xl font-semibold">Your trip is waiting for a first place.</h2><p className="mx-auto mt-3 max-w-xl leading-7 text-[#66756f]">Open the Trip Builder, choose your pace and interests, then save the journey here.</p><a href="/plan" className="mt-7 inline-block rounded-full bg-[#d9a441] px-7 py-3.5 font-bold">Build my trip →</a></div>:<><div className="mt-12 grid gap-4 sm:grid-cols-2 lg:grid-cols-5"><div className="rounded-3xl bg-white p-6"><p className="text-xs font-bold uppercase tracking-widest text-[#8d651d]">Duration</p><p className="mt-2 text-2xl font-semibold">{days} days</p></div><div className="rounded-3xl bg-white p-6"><p className="text-xs font-bold uppercase tracking-widest text-[#8d651d]">Travelers</p><p className="mt-2 text-2xl font-semibold">{travelers}</p></div><div className="rounded-3xl bg-white p-6"><p className="text-xs font-bold uppercase tracking-widest text-[#8d651d]">Style</p><p className="mt-2 text-2xl font-semibold">{budget}</p></div><div className="rounded-3xl bg-white p-6"><p className="text-xs font-bold uppercase tracking-widest text-[#8d651d]">Interest</p><p className="mt-2 text-2xl font-semibold">{interest}</p></div><div className="rounded-3xl bg-[#183d32] p-6 text-white"><p className="text-xs font-bold uppercase tracking-widest text-[#e7c36e]">Estimated total</p><p className="mt-2 text-2xl font-semibold">{money(Number(estimate))}</p></div></div><section className="mt-8 rounded-[2rem] bg-white p-7 shadow-sm md:p-9"><div className="flex flex-col gap-5 md:flex-row md:items-center md:justify-between"><div><p className="text-sm font-bold uppercase tracking-widest text-[#8d651d]">Journey snapshot</p><h2 className="mt-2 text-3xl font-semibold">{trip?.title??"Your saved Sri Lanka route"}</h2><p className="mt-2 text-[#66756f]">{route}</p></div><div className="min-w-56 rounded-2xl bg-[#f7f5ef] p-4"><div className="flex items-center justify-between text-xs font-bold uppercase tracking-widest"><span>Plan coverage</span><span>{Math.min(100,Math.round(selected.length/Math.max(1,days)*100))}%</span></div><div className="mt-3 h-2 rounded-full bg-black/10"><div className="h-2 rounded-full bg-[#183d32]" style={{width:`${Math.min(100,Math.round(selected.length/Math.max(1,days)*100))}%`}}/></div></div></div><div className="mt-7 grid gap-3 md:grid-cols-3"><div className="rounded-2xl bg-[#f7f5ef] p-4"><p className="text-xs uppercase tracking-widest text-[#8d651d]">Average / day</p><p className="mt-1 text-xl font-semibold">{money(Number(estimate)/Math.max(1,days))}</p></div><div className="rounded-2xl bg-[#f7f5ef] p-4"><p className="text-xs uppercase tracking-widest text-[#8d651d]">Average / traveler / day</p><p className="mt-1 text-xl font-semibold">{money(Number(estimate)/Math.max(1,days*travelers))}</p></div><div className="rounded-2xl bg-[#f7f5ef] p-4"><p className="text-xs uppercase tracking-widest text-[#8d651d]">Sync</p><p className="mt-1 text-xl font-semibold">{trip?"Live":"Browser only"}</p></div></div></section>{itinerary?.content?.days?.length?<section className="mt-10 rounded-[2rem] bg-white p-7 shadow-sm md:p-10"><div className="flex items-end justify-between gap-4"><div><p className="text-sm font-bold uppercase tracking-widest text-[#8d651d]">Live itinerary</p><h2 className="mt-2 text-3xl font-semibold">{itinerary.title}</h2><p className="mt-2 text-sm text-[#66756f]">{itinerary.summary}</p></div><a href="/plan" className="hidden text-sm font-bold text-[#183d32] sm:block">Refine →</a></div><div className="mt-7 space-y-3">{itinerary.content.days.map(day=><article key={day.day} className="rounded-2xl border border-black/5 bg-[#f7f5ef] p-5"><div className="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-widest text-[#8d651d]">Day {day.day}</p><h3 className="mt-1 text-xl font-semibold">{day.destination?.name??"Sri Lanka"}</h3></div><p className="text-sm text-[#66756f]">{day.destination?.region??"Sri Lanka"} · {day.focus??"Travel"}</p></div><div className="mt-4 grid gap-2 md:grid-cols-3">{day.items?.map((item,index)=><div key={`${item.time??"plan"}-${index}`} className="rounded-xl bg-white p-3"><p className="text-xs font-bold text-[#b27c1d]">{item.time??"Plan"}</p><p className="mt-1 text-sm font-semibold">{item.title??"Explore locally"}</p></div>)}</div></article>)}</div></section>:null}<div className="mt-12 flex items-center justify-between border-b border-black/10 pb-5"><div><p className="font-semibold">{selected.length} {selected.length===1?"place":"places"} saved</p><p className="mt-1 text-sm text-[#66756f]">Remove a stop or refine the route in Trip Builder.</p></div><button onClick={clearLocal} className="text-sm font-semibold text-[#8d651d]">Clear local trip</button></div><div className="mt-7 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{selected.map((place,index)=><article key={place.slug} className="overflow-hidden rounded-3xl bg-white shadow-sm"><div className="relative h-56"><img src={place.image} alt={place.name} className="h-full w-full object-cover"/><span className="absolute left-4 top-4 rounded-full bg-white/90 px-3 py-1 text-xs font-bold">Stop {index+1}</span><button onClick={()=>removeStop(place.slug)} className="absolute right-4 top-4 rounded-full bg-black/60 px-3 py-1 text-xs font-bold text-white">Remove</button></div><div className="p-6"><p className="text-xs font-bold uppercase tracking-widest text-[#8d651d]">{place.region}</p><h2 className="mt-1 text-2xl font-semibold">{place.name}</h2><p className="mt-2 text-sm leading-6 text-[#66756f]">{place.summary}</p><div className="mt-5 flex items-center justify-between"><a href={`/destinations/${place.slug}`} className="text-sm font-bold text-[#183d32]">Explore →</a><a href={`/plan?destination=${encodeURIComponent(place.slug)}`} className="text-sm font-semibold text-[#8d651d]">Plan here</a></div></div></article>)}</div><div className="mt-10 rounded-[2rem] bg-[#10251f] p-8 text-white md:p-10"><p className="text-sm font-bold uppercase tracking-widest text-[#e7c36e]">Next move</p><h2 className="mt-2 text-3xl font-semibold">Make the route more yours.</h2><p className="mt-2 max-w-xl text-white/60">Change days, travelers, interests or budget in the Trip Builder. The next generated plan replaces the live itinerary.</p><a href="/plan" className="mt-6 inline-block rounded-full bg-[#d9a441] px-7 py-3.5 font-bold text-[#10251f]">Refine my trip →</a></div></>}</section></main>;
}
